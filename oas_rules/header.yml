rules:

  header-names:
    description: print header names
    message: {{description}}
    severity: warn
    given: $.paths.*.*.parameters[?(@.in == "header")].name
    then:
      function: print
