rules:

  header-names:
    description: remove content type header
    message: {{description}}
    severity: warn
    given: $.paths.*.*.parameters[?(@.in == "header")].name
    then:
      function: print
